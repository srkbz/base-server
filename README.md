# srk.bz Infrastructure

Each folder inside `pkgs` is a different APT package that solves an
infrastructure piece.

```bash
# See available tasks
./task
# or ./task help

# Build a package
./task build <pkg name>

# Install locally a package
./task install <pkg name>
```
